// Пагинация. Взято с сайта swiperjs.com
const swiper = new Swiper(".swiper", {
    pagination: {
      el: ".swiper-pagination",
    },
  });

  // Не работает почему-то. Пересмотри с момента добавления пагинации.